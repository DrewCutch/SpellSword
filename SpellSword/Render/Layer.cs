using System;
using System.Collections.Generic;
using System.Drawing;
using System.Text;

namespace SpellSword.Render
{
    /*
    public sealed class Layer
    {
        public static Layer Background = new Layer(0);
        public static Layer Low = new Layer(1);
        public static Layer Main = new Layer(2);
        public static Layer High = new Layer(3);

        public static IEnumerable<Layer> AllLayers()
        {
            yield return Background;
            yield return Low;
            yield return Main;
            yield return High;
        }

        public const int Layers = 4;

        public readonly int MapNum;
        public readonly int Num;
        public readonly uint Mask;

        private Layer(int layerNum)
        {
            MapNum = layerNum - 1;
            Num = layerNum;
            Mask = (uint) (1 << Num);
        }
    }
    */
}
